using ConsoleApp.Challenges.Graphs;
using System;
using System.Collections.Generic;
using Xunit;

namespace GraphTest
{
    public class UnitTest1
    {

        //       
        //
        //A collection of all nodes can be properly retrieved from the graph
        //All appropriate neighbors can be retrieved from the graph
        //Neighbors are returned with the weight between nodes included
        //The proper size is returned, representing the number of nodes in the graph
        //A graph with only one node and edge can be properly returned
        //An empty graph properly returns null

        // 1-  Node can be successfully added to the graph
        //[Fact]
        //public void CanAddNode()
        //{
        //    Graph graph = new Graph();
        //    graph.AddNode(4);
        //    Assert.NotEmpty(graph.getNodes());
        //}
        // 2- An edge can be successfully added to the graph
        //[Fact]
        //public void EdgeCanBeAddedToGraph()
        //{
        //    Graph graph = new Graph();
        //    GraphNode A = graph.AddNode(4);
        //    GraphNode B = graph.AddNode(6);
        //    GraphNode C = graph.AddNode(8);


        //    graph.AddEdge(A, B);

        //    Assert.NotEmpty(A.Neighbors);
        //}
    }
    }

